
name: Test

on:
  push:
    branches:
      - 'master'

jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    env:
      FALSE: false

    steps:
    
    - name: echo1
      run: echo ${{ steps.stepid.outputs.value }} 
    
    - name: echo2
      run: echo ${{ env.FALSE != false }}
